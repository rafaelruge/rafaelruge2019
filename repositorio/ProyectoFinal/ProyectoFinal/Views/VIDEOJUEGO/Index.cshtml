@model IEnumerable<ProyectoFinal.Models.VIDEOJUEGO>

@{
    ViewBag.Title = "Index";
}

<link rel="stylesheet" type="text/css" href="~/Content/Style.css">
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_VIDEOJUEGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORIA_VIDOJUEGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PESO_VIDEOJUEGO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORIA_EDAD)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NOMBRE_VIDEOJUEGO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CATEGORIA_VIDOJUEGO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PESO_VIDEOJUEGO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CATEGORIA_EDAD)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_VIDEOJUEGO }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_VIDEOJUEGO }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_VIDEOJUEGO })
            </td>
        </tr>
    }

</table>
