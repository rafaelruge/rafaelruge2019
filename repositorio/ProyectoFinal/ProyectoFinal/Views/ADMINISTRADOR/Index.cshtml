@model IEnumerable<ProyectoFinal.Models.ADMINISTRADOR>

@{
    ViewBag.Title = "Index";
}
<link rel="stylesheet" type="text/css" href="~/Content/Style.css">
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_ADMINISTRADOR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.APELLIDO_ADMINISTRADOR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CORREO_ADMINISTRADOR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_ADMINISTRADOR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.APELLIDO_ADMINISTRADOR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CORREO_ADMINISTRADOR)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ADMINISTRADOR }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ADMINISTRADOR }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ADMINISTRADOR })
        </td>
    </tr>
}

</table>
